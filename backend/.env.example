# Server Configuration
PORT=8080

# JWT Secret (Change this in production!)
JWT_SECRET=mexico-secret-key-change-in-production

# Database (Future: when implementing persistent storage)
# DATABASE_URL=
